---
allowed-tools: Bash(git init :*), Bash(git remote:*), Bash(git add:*), Bash(git commit:*)
description: 初始化一个git仓库
---


## your task

1. 初始化一个新git仓库，执行“git init”
2. 如果项目目录内有文件，创建一个初始化提交
    - 用“git add -A"暂存文件
    - 创建提交，信息为”initial commit“
3. 展示做了什么
